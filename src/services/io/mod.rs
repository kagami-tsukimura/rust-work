use crate::models;
use std::fs::File;
use std::io::prelude::*;
use std::io::BufReader;

pub fn read_data_or_create_new_data(file_path: &str) -> Vec<models::Item> {
    println!("read_data_or_create_new_data");
}
